overwrite: true
schema: "https://petbook-back-dev.herokuapp.com/graphql"
documents: "src/**/*.graphql"
generates:
  src/graphql/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
